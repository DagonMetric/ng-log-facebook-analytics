resources:
  repositories:
    - repository: templates
      type: github
      name: DagonMetric/ng-workspace-configs
      endpoint: github.com_dagonmetric-contributor

stages:
  - template: azure-pipelines/multi-platform-build-stage.yml@templates
